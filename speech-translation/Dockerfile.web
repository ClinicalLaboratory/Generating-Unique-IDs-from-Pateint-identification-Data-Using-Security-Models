# Use nginx for serving the web interface
FROM nginx:alpine

# Copy web files to nginx html directory
COPY web/ /usr/share/nginx/html/

# Copy custom nginx configuration
COPY nginx.conf /etc/nginx/nginx.conf

# Expose port 80
EXPOSE 80

# Start nginx
CMD ["nginx", "-g", "daemon off;"]